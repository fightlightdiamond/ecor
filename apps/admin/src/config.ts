// Cấu hình admin.
// Tạm tắt đa ngôn ngữ: form chỉ nhập 1 ngôn ngữ (tiếng Việt). Dữ liệu vẫn lưu
// dạng { vi, en } để bật lại sau dễ dàng — chỉ ẩn ô tiếng Anh ở giao diện.
export const MULTILANG = false;

// Ngôn ngữ duy nhất khi tắt đa ngôn ngữ.
export const PRIMARY_LOCALE = 'vi';
